{
  "name": "final",
  "version": "1.0.0",
  "description": "Templating",
  "main": "server.js",
  "scripts": {
    "build": "handlebars views/partials/postTemplate.handlebars -f public/postTemplate.js",
    "prestart": "npm run build",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.16.4",
    "express-handlebars": "^3.0.0",
    "handlebars": "^4.0.12"
  }
}
